{
  "version": 2,
  "#": "This config runs the repo-root build, then copies the generated dist/spa into client/dist so Vercel (when Root Directory is 'client') can serve it.",
  "buildCommand": "pnpm --prefix .. run build && cp -r ../dist/spa ./dist",
  "outputDirectory": "dist",

  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

